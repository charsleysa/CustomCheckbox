<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:CheckboxSample"
             xmlns:components="clr-namespace:CheckboxSample.Components"
             xmlns:ViewModel="clr-namespace:CheckboxSample.ViewModels"
             x:Class="CheckboxSample.MainPage">
  <StackLayout>
    <ListView 
      ItemsSource="{Binding Users}"
      VerticalOptions="Center"
      HorizontalOptions="Center">
      <ListView.ItemTemplate>
        <DataTemplate>
          <components:CheckBoxCell Text="{Binding UserName}" IsChecked="{Binding IsSelected, Mode=TwoWay}"></components:CheckBoxCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
    <Button
      Text="Select all"
      Command="{Binding AllCommand}">
    </Button>
  </StackLayout>
</ContentPage>